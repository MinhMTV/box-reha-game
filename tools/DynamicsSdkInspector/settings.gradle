pluginManagement { repositories { google(); mavenCentral(); gradlePluginPortal() } }
dependencyResolutionManagement {
 repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
 repositories {
  maven { url = uri(providers.gradleProperty('dynamicsMavenPath').orElse(providers.environmentVariable('DYNAMICS_MAVEN_PATH')).getOrElse('C:/dynamics-sdk-main/mavenLocal')) }
  google(); mavenCentral()
 }
}
rootProject.name = 'DynamicsSdkInspector'
include ':app'
