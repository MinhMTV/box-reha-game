# dynamics-sdk

The API documentation is generated with [Dokka](https://github.com/Kotlin/dokka) and located at `/docs`.

For the best viewing experience, it is recommended to host `/docs` on a local web server and open `/docs/index.html`.