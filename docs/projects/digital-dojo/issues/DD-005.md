# DD-005 — Android integration qualification

Priority: P0/P1

## Outcome
Run actual native/Unity Android toolchain; fix internally solvable build and lifecycle failures.

## Why
Provide the corresponding observable delivery gate in SPEC.md.

## Dependencies
DD-001

## In Scope
Run actual native/Unity Android toolchain; fix internally solvable build and lifecycle failures.

## Out of Scope
New game, research scope changes, publishing, fabricated hardware or clinical results.

## Acceptance Criteria
Actual build result recorded; supported dependencies preserved; permission/lifecycle failures distinguishable; physical tests separately qualified.

## Verification
Native compiler, Unity generated Gradle, APK attempt and host integrity tests

## Expected Files
Android plugin, Gradle export hook, build scripts, Android status

## Risks
Supplied SDK publication defects and missing hardware. Preserve existing source provenance and user Gradle cache changes.

## Rollback Strategy
Each slice lands as an isolated commit; revert that commit on a review branch if required. Never reset unrelated work.

## Execution result — 2026-09-14
Actual native and Unity Android builds executed. Externally blocked by missing vendor resource 2.11.1. C# compilation and generated Gradle integration are evidenced; no successful APK or physical JNI/BLE session. See root ANDROID_STATUS.md. Remains BLOCKED for hardware delivery.
